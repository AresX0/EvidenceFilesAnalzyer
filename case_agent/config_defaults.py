"""Central configuration defaults (paths, models, intervals).

Module-level defaults are intended to be safe and documented; users may
override values by setting attributes in `case_agent.config` or via the GUI.
"""

# Optional PDF viewer path (prefer open-source options like SumatraPDF on Windows)
PDF_VIEWER = None

# Note: users may override values by setting attributes in case_agent.config module
