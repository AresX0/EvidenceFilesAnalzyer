Unreleased changes

- GUI: Added thumbnail gallery, clickable thumbnails with preview + context menu, sorting/filtering, and per-person CSV export with thumbnails and SHA256. ✅
- GUI: Added tabs for All / Images / Documents, debounced search, document list with double-click open, and PDF preview support. ✅
- Utilities: Added `case_agent.utils.thumbs` for cached thumbnail generation and PDF-first-page rendering; added `case_agent.utils.image_overlay` for drawing face match overlays. ✅
- Reports: `write_report_html` now separates People into Images and Documents sections, plus per-person pages with thumbnails and document links. ✅
- Tests: Added unit tests for thumbnail utilities, image overlay, GUI import/smoke test, and HTML report generation. ✅
- Agent: Added a lightweight HTTP API (`case_agent.api`) exposing `/agent/*` and `/reports/people` endpoints, plus a `serve` CLI and unit tests. ✅
- CLI: Added `people-report` subcommand to export aggregated people-mention reports (JSON/CSV). ✅
- GUI: Added quick Agent query bar and a People Mention Report viewer with export. ✅
- Tests: Added headless GUI integration tests (`tests/test_gui_integration.py`) and integration fixture in `tests/conftest.py`. ✅
- CI: Added GitHub Actions workflow `.github/workflows/gui-integration.yml` to run tests on push/PR. ✅
- Next: Implement further performance improvements (true virtualization), keyboard shortcuts refinements, and settings UI for PDF viewer. 🔜
