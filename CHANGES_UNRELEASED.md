Unreleased changes

- GUI: Added thumbnail gallery, clickable thumbnails with preview + context menu, sorting/filtering, and per-person CSV export with thumbnails and SHA256. ✅
- Utilities: Added `case_agent.utils.thumbs` for cached thumbnail generation. ✅
- Reports: `write_report_html` now produces per-person HTML pages and inline thumbnails in the main report. ✅
- Tests: Added unit tests for thumbnail utilities, GUI import/smoke test, and HTML report generation. ✅
